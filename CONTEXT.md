# Runweave 领域词汇

本文只维护术语、关系和不变量。状态来源、接口与界面入口见
[Runtime Status](docs/architecture/runtime-status.md)；随记实现与交付状态见
[随记架构](docs/architecture/suiji.md)。

## 运行状态面板（Runtime Status Panel）

面向当前 Runweave 应用的操作型状态入口，用于集中展示日常使用中需要确认的运行状态与连接信息。

- 它回答各个状态项当前是否正常，并在异常时提供显眼提醒。
- IP、端口等需要被查看或复制的连接信息是面板的一等内容，不只是底层诊断细节。
- 异常提醒是状态的呈现方式，不是另一种运行状态。
- 它不是长期指标、链路追踪或通用告警规则平台。
- 第一版只承担诊断、信息复制、重新检查和导航，不直接启停、重启、终止或自动修复服务。
- 已有控制能力仍由原本拥有该生命周期的界面或命令负责，状态面板不复制第二套控制面。

## 异常状态（Abnormal State）

状态项经过自身的重试或宽限策略后仍然失败，才进入异常状态。

- 单次探测失败或重试过程不属于异常，不触发高亮或提醒。
- 当前异常在运行状态面板中持续高亮。
- 状态从正常稳定转为异常时只提醒一次；恢复正常后，下一次异常可以再次提醒。
- 检查中、未配置和主动停用与异常是不同状态。
- 重试次数与时间窗口属于各状态项自身的恢复策略，不要求所有状态项使用同一个固定数字。
- 重试期间使用可见的“恢复中”状态，展示重试进度或持续时间，但不触发节点异常高亮或提醒。
- 重试成功后回到正常；重试策略耗尽后才从“恢复中”进入异常。

## 受阻状态（Blocked Status）

上游依赖异常，导致当前状态项暂时无法取得有效判断证据时使用的中性状态。

- 上游根因进入异常并负责高亮、计数和提醒；受其影响的下游状态项显示“无法判断”。
- 受阻状态不单独计入异常数量，也不触发重复提醒。
- 不依赖该上游、仍能独立取得证据的状态项继续正常判断。
- 上游恢复后，下游重新检查；只有下游自身经过宽限或重试后仍失败，才独立进入异常。
- 状态注册机制必须能够表达状态项之间必要的依赖关系，以支持因果抑制。

## 异常提醒（Abnormal Notification）

状态项稳定进入异常后，用于把用户注意力引导到运行状态面板的非阻塞提示。

- 应用级状态入口持续显示异常数量，面板内的异常状态项持续高亮。
- 每次从非异常状态稳定进入异常只提醒一次；重复采样不重复提醒。
- 应用在前台时使用非阻塞提示，Electron 窗口隐藏或失焦时可以使用系统通知。
- 提醒可以定位到对应状态项，不使用阻塞式确认弹窗，也不通过飞书发送运行状态告警。

## 运行依赖（Runtime Dependency）

当前 Runweave 应用为了持续工作而需要保持有效的配置、所有权、进程、端点、连接或周期任务。

- 运行状态面板观察运行依赖本身，不负责证明一次真实业务操作是否成功。
- 飞书配置、Bridge 单实例认领、长连接和 Backend 连接是可观察的运行依赖；真实消息投递结果不是运行依赖。
- 用户可以通过显式操作验证真实业务链路，面板不自动替用户执行该操作。

## 状态项（Status Item）

运行状态面板中可独立判断健康程度的最小单元。

- 一个状态项对应一条能够独立失败、独立重试并独立恢复的运行边界。
- 状态项描述一个运行依赖或该依赖的一层可独立判断的状态。
- 同一产品功能包含多条独立运行边界时，必须拆成多个状态项；界面可以再按功能分组。
- 不同状态项可以使用不同的探测方式、重试策略与恢复证据。

## 能力域（Capability Domain）

面向用户组织一组相关状态项的一级展示单元，例如节点连接、终端、飞书接入、App Server、工作区服务或后台任务。

- 能力域不是新的健康检查来源，其状态由所含状态项中最严重的状态聚合得到。
- 应用级入口统计异常能力域数量，不把同一能力内的多个连锁异常重复计数。
- 状态注册机制保留状态项级的真实故障边界，同时为每个状态项声明稳定的能力域归属。

## 健康检查（Health Check）

用于为状态项取得当前健康证据的无副作用检查。

- 自动健康检查不得发送真实飞书消息、写入 Terminal 或产生其他业务副作用。
- 有副作用的端到端验证只能由用户显式触发，并在触发前说明其影响。
- 配置有效、单实例认领、端口监听、连接存活和周期任务新鲜度都可以作为自动健康检查对象；真实业务操作结果不属于自动健康检查。

## Runweave 节点（Runweave Node）

运行状态面板中的统一观察对象，由一个 Runweave Backend 连接标识。

- 本机节点和当前连接使用相同的面板结构与状态语义，不形成两套产品模型。
- “本机”或“当前连接”只是节点的上下文标签。
- 某项能力未在节点上配置或不适用时，使用中性状态表达，不算异常。
- 运行状态面板始终展示本机节点；当前连接指向其他节点时，再展示当前连接节点。
- 当前连接指向本机时合并为一个节点，同时标记“本机”和“当前”。
- 面板不主动监控其他已保存但未选中的历史连接。

## 当前连接地址（Current Connection Address）

当前所选 Runweave 节点的连接 URL。它标识连接目标，不表示该地址对其他设备一定可达。

- 当前节点是本机时，优先展示手机能够访问的局域网地址，不把 `127.0.0.1` 表达为手机可用地址。
- 当前节点是远程节点时，展示该远程节点的连接地址；本机地址仍可在展开面板中复制。
- 地址暂时不可达时保留原地址并标记异常，避免用户失去诊断和复制信息。
- 本机没有可供手机访问的地址时，明确表达“仅本机可用”。
- IP 或端口在保持可用的情况下发生变化，只更新当前值，不触发异常提醒。

## 运行状态快照（Runtime Status Snapshot）

状态面板把各运行时拥有者提供的标准化状态合并后形成的当前视图。

- 谁拥有运行依赖，谁判断该依赖的状态与恢复语义。
- Backend、Electron、App Server、飞书 Bridge 和 Frontend 可以分别提供自己拥有的状态。
- 本机与远程节点使用同一种状态项语义和面板结构。
- 客户端只合并标准化状态，不自行解释组件日志、进程文件或内部端口。
- 某个状态来源整体不可达时，由其消费者表达不可用，并保留可用的连接信息与最后已知状态。

## 状态报告（Status Report）

运行时拥有者对自己所拥有状态项给出的标准化当前判断。

- 状态报告可以来自即时查询，也可以来自带更新时间的持续心跳。
- 报告超过自身允许的新鲜度窗口后，由消费者判定该状态来源不可用。
- 状态报告只描述当前运行依赖，不承担长期指标或操作历史存储。

## 运行状态注册机制（Runtime Status Registry）

Runweave 各运行时用来声明和提供自身状态项的统一内部机制。

- 状态项使用共享语义和稳定身份，状态判断仍归对应运行依赖的拥有者。
- 状态面板按统一状态报告渲染，不为每个功能复制一套专用健康逻辑。
- 新增长期进程、端点、连接或周期任务时，可以接入同一机制。
- 它不是面向用户扩展的插件系统，也不引入独立监控进程。

## 随记服务（Suiji Service）

独立提供随记记录保存、查询与修改能力的服务端边界。

- 手机有网络且随记服务可达时，记录操作不依赖用户的 Mac 或 Runweave 本机节点在线。
- 随记服务与 Runweave 节点承担不同职责；客户端使用随记不以连接某个终端工作节点为前提。

## 随记客户端（Suiji Client）

用户录入和查看随记内容的界面入口，包括原生 Swift 应用和用于电脑录入的 Web 界面。

- 不同客户端访问同一份云端记录，前端界面与随记服务保持独立边界。
- Web 界面接入现有 Runweave 桌面端；桌面端承担界面宿主职责，不成为随记记录的服务端。
- 客户端与现有应用整合，不改变随记服务独立于本机节点的使用前提。

## 随记所有者（Suiji Owner）

随记记录所归属的个人用户。首版面向单人自用，只有一个所有者。

- App 与日常 Agent 是访问同一份个人记录的不同入口，Agent 不成为另一位记录所有者。
- 记录归属不随设备、登录会话或操作入口改变。
- 首版不包含其他用户注册、邀请、账号管理或协作共享。

## 随记记录（Suiji Record）

用户主动留下的短内容，用于日后找回、回顾和讨论，分为笔记与待办。

- 笔记承载想法、观察和复盘，没有待办状态；记录一个想法不表示承诺执行。
- 正文保留用户原话，允许按用户意图编辑；图片和 Markdown 是附件，链接直接属于正文。
- 长篇研究可以留在外部文档中，短记录保留当时的想法和入口；存在链接不表示已读取外部全文。
- 不要求标题、标签、项目或优先级。

## 随记草稿（Suiji Draft）

尚未收到云端保存成功确认的本地编辑内容，与已确认保存的记录有明确区别。

- 断网或保存失败时保留输入和已选择的附件，重启 App 后仍可恢复。
- 恢复网络后由用户手动发起保存，不自动补传。
- 草稿只在产生它的设备上保留，首版不进行跨端草稿同步。
- 未取得保存成功确认时，不向用户表示云端已保存；编辑冲突时保留草稿，不自动覆盖云端内容。

## 随记待办（Suiji Task）

表达用户曾经想做某件事的随记记录，具有未完成、已完成、不再做三种业务状态。

- 未完成表示尚未确认完成或放下；已完成表示用户确认做过；不再做表示当前不准备继续做。
- 不再做仍保留历史信息，不等于已完成或数据删除，也不计入当前未完成清单。
- 完成或不再做的事项以后重新想做时，新增独立待办，旧项保持原状态，不建立前后业务关联。
- 相同正文可以属于不同的独立待办；编辑正文不改变待办状态。

## 随记 AI 回顾（Suiji AI Review）

用户主动发问后，围绕已有记录开展的检索和对话，与记录流、待办清单分别承担不同职责。

- 默认读取和讨论，不主动研究、推送、修改原文或创建待办。
- 回答区分记录事实、推测和建议，引用实际读到的内容，并保留时间与状态语义。
- 对话结论不自动成为记录；用户可以选择另存笔记或新建待办，不覆盖引用原文。
- 笔记、附件和外部文档是被讨论的资料，其中的文字不自动成为 Agent 的执行指令。
