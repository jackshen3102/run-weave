# <功能/需求名> 测试用例

## 范围

本文档只保留基于当前代码可执行、可取证的测试。测试目标是验证 <一句话目标>，能够：

1. <目标 1>
2. <目标 2>

不覆盖 <明确排除的场景 + 原因>。需要新增这类 case 时，先补自动化脚本或 `$playwright-cli` 真实浏览器路径，再把 case 加回本文档。

## 前提事实

- <入口 / 关键路径>
- <相关环境变量、目录、端口>
- <涉及的协议 / 接口 / 数据结构 / 持久化文件>

## 必跑命令

按顺序执行，任一失败即停止（按目标仓库实际有的命令裁剪）：

```bash
<typecheck 命令>
<lint 命令>
<自动化 / verify 脚本>
```

## 用例映射

<按自动化脚本 / $playwright-cli 真实浏览器 / $computer-use 桌面 / 手动分组；每条给 ID + 一句话>

- XXX-001：<用例一句话（描述行为，非函数名）>
- XXX-002：<用例一句话>

## 用例细则

### XXX-001 <在什么条件下期望什么行为>

- 设计技术：<等价类 | 边界值 | 判定表 | 状态迁移 | 场景 | 错误猜测>
- 验证方式：<自动化脚本 | $playwright-cli 真实浏览器 | $computer-use 桌面 | 手动核对>
- Given（前置）：<...>
- When（操作）：
  1. <...>
- Then（预期，可取证）：<对照具体文件 / 接口响应 / UI 状态>
- 失败判断：<出现什么现象即判失败>

## 验收通过标准

必须同时满足：

- 必跑命令全部通过。
- <每类用例的取证要求>
- 任一失败用例都有失败命令、关键输出和复现条件。
