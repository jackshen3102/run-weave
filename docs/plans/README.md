# plans（设计计划归档）

按日期沉淀的功能设计计划稿（`YYYY-MM-DD-<feature>.md`）。

- **性质**：一次性产物，写于实现之前，落地后即冻结。
- **不代表当前事实**：系统的当前状态以 `docs/architecture/` 为准；这里只记录“当时打算怎么做”。
- **不需保鲜**：不要回头修改已冻结的计划稿；实现偏离计划时，更新的是架构文档，不是这里。
- 检索当前该怎么做，请优先读 `docs/README.md` 指向的活文档。
